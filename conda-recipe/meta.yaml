package:
  name: wera2netcdf
  version: "0.0.3"

source:
  fn: wera2netcdf-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/w/wera2netcdf/wera2netcdf-0.0.3.tar.gz
  md5: 1970a907d222905c262619d420cc4f8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wera2netcdf = wera2netcdf:main
    #
    # Would create an entry point called wera2netcdf that calls wera2netcdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - pandas
    - netcdf4
    - pygc
    - pytz
    - python-dateutil

  run:
    - python
    - pytest
    - pandas
    - netcdf4
    - pygc
    - pytz
    - python-dateutil

test:
  # Python imports
  imports:
    - wera2netcdf
    - wera2netcdf.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - pytest
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/axiom-data-science/wera2netcdf
  license: MIT License
  summary: 'A utility to convert WERA total ASCII files into CF NetCDF files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
